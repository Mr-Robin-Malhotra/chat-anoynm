// Production config. Set this to the deployed C relay's WebSocket URL.
// Leave it empty for local development (the app falls back to ws://localhost:8080).
// Example after deploying the relay to Fly.io:
//   window.RELAY_URL = "wss://chat-anoynm-relay.fly.dev";
window.RELAY_URL = "wss://chat-anoynm.onrender.com";
